# Generated gRPC stubs live under this package. protoc emits absolute imports
# ("from stemma.v1 import ..."), so this directory joins sys.path once.
import os
import sys

_here = os.path.dirname(__file__)
if _here not in sys.path:
    sys.path.append(_here)
