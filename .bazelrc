# Keep host environment out of actions for reproducibility.
build --incompatible_strict_action_env

test --test_output=errors

# Local convenience overrides go in user.bazelrc (gitignored).
try-import %workspace%/user.bazelrc
