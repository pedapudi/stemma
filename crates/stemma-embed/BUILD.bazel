load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "stemma-embed",
    srcs = ["src/lib.rs"],
    edition = "2021",
    visibility = ["//visibility:public"],
    deps = [
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:thiserror",
        "@crates//:ureq",
    ],
)
