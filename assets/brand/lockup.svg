<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 106 24" width="106" height="24"
     role="img" aria-label="stemmadb" color="#15181C">
  <title>stemmadb</title>
  <!-- mark + wordmark, horizontal. the gap is 9 units — a third of the mark's
       width, the same rhythm the console topbar uses. the wordmark is pinned to
       JetBrains Mono 700, lowercase, always. ink is currentColor throughout; the
       archetype dot is the only colored element in the lockup.
       the color="" above is a FALLBACK for <img> use; inline hosts should drop
       it (or set `svg { color: inherit }`) so the lockup follows the page ink. -->
  <g fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
    <path d="M4 20 C4 13 9 12 12 8"/>
    <path d="M12 20 C12 15 12 12 12 8"/>
    <path d="M20 20 C20 13 15 12 12 8"/>
    <path d="M7.5 15.5 L9.2 17.2"/>
    <path d="M16.5 15.5 L14.8 17.2"/>
  </g>
  <circle cx="12" cy="5.6" r="2.1" fill="var(--brand-accent, #2FA46A)"/>
  <text x="33" y="17" fill="currentColor"
        font-family="JetBrains Mono, ui-monospace, SFMono-Regular, Menlo, Consolas, monospace"
        font-weight="700" font-size="14" letter-spacing="0.14">stemmadb</text>
</svg>
